apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    app: ingest-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app: ingest-service
  ports:
    - name: grpc
      port: {{ .Values.service.port }}
      targetPort: 50051